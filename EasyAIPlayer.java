public class EasyAIPlayer extends AIPlayer
{
    //constructor (just super)

    //makeMove() - returns true when made, false otherwise
        //checks for win, plays the win 75% of the time
            //if there's a win, does the following:

                //use a while loop to run a random number 1-4, if 1-3, plays the win, 4 does not
                //randomMove()

        //checks for block, plays the block 40% of the time
            //if there's a block, does the following:

                //use a while loop to run a random number 1-5, if 1-2, plays the block, 3-5 does not
                //randomMove()

        //randomMove()
            //else:
                //randomMove()

}